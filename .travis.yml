sudo: required 
services :
- docker
script:
- docker build -t python:latest .
- docker images 
before_deploy:
  - docker login -u amelgm -p amoulache
deploy:
  provider:script
  script:
  docker push amelgm/django
  on:
    master